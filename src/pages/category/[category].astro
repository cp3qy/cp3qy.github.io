---
import { getCollection } from 'astro:content'
import BaseLayout from '../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  // 获取所有文章并提取唯一分类
  const allPosts = await getCollection('posts');
  const categories = [...new Set(allPosts.map(post => post.data.category || 'tech'))];

  // 为每个分类生成路径
  return categories.map(category => ({
    params: { category },
    props: { category, allPosts }
  }));
}

const { category } = Astro.params;
const allPosts = Astro.props.allPosts;

// 过滤当前分类的文章
const categoryPosts = allPosts.filter(post => post.data.category === category);
const sortedPosts = categoryPosts.sort((a, b) => new Date(b.data.publishDate).valueOf() - new Date(a.data.publishDate).valueOf());

const title = `${category.charAt(0).toUpperCase() + category.slice(1)} Category`;
const description = `Articles in the ${category} category.`;
const permalink = `${Astro.site.href}category/${category}`;

// 定义状态标签
const statusLabels = {
  'to-read': 'To Read',
  'reading': 'Reading',
  'finished': 'Finished',
  'abandoned': 'Abandoned'
};

// 获取所有唯一分类用于导航
const allCategories = [...new Set(allPosts.map(post => post.data.category || 'tech'))];

// 为所有书籍分类准备数据，客户端将处理过滤
const allBooksData = JSON.stringify(sortedPosts.map(post => ({
  ...post,
  data: {
    ...post.data,
    publishDate: post.data.publishDate instanceof Date
      ? post.data.publishDate.toISOString()
      : post.data.publishDate
  }
})));
---

<BaseLayout title={title} description={description} permalink={permalink} current="category">
  <div class="container">

    <!-- 显示分类文章 -->
    {sortedPosts.length > 0 ? (
      category === 'books' ? (
        // 书籍分类使用标签页布局，通过客户端JavaScript处理过滤
        <div class="books-container">
          <div class="books-tabs">
            {['all', 'to-read', 'reading', 'finished', 'abandoned'].map(status => {
              const count = status === 'all'
                ? sortedPosts.length
                : sortedPosts.filter(post => post.data.readingStatus === status).length;

              return (
                <button
                  class="tab-button"
                  data-status={status}
                >
                  {status === 'all' ? 'All' : statusLabels[status]}
                  <span class="tab-count">{count}</span>
                </button>
              );
            })}
          </div>

          <div class="books-grid" id="books-grid">
            {sortedPosts.map((post) => {
              // [修改] 生成唯一的 transition name，用于 View Transitions API 追踪元素位置
              const transitionName = `book-${post.data.slug.replace(/[^a-zA-Z0-9-]/g, '-')}`;
              
              return (
                <div
                  class="book-card visible"
                  data-status={post.data.readingStatus || 'to-read'}
                  style={`view-transition-name: ${transitionName}`}
                >
                  <div>
                    {post.data.coverImage ? (
                      <img
                        src={post.data.coverImage}
                        alt={post.data.title}
                        class="book-cover"
                        onError={(e) => {
                          // 如果图片加载失败，显示占位图
                          e.target.src = '/images/books/placeholder.svg';
                        }}
                      />
                    ) : (
                      <div class="book-cover-placeholder">
                        <span>No Cover</span>
                      </div>
                    )}

                    <div class="book-info">
                      <h3>{post.data.title}</h3>
                    </div>
                  </div>
                </div>
              )
            })}
          </div>
        </div>
      ) : (
        // 非书籍分类使用原始布局
        sortedPosts.map((post, index) => {
          const href = `/post/${post.data.slug}`;
          return (
            <div>
              { index !== 0 && <hr /> }
              <div class="post-item">
                <h2>
                  <a href={href}>{post.data.title}</a>
                </h2>
                <div class="post-item-footer">
                  <span class="post-item-date">— {post.data.publishDate}</span>
                  <span class="post-category">— {post.data.category}</span>
                </div>
              </div>
            </div>
          )
        })
      )
    ) : (
      <p>No posts found in this category.</p>
    )}
  </div>
</BaseLayout>

<script>
  // 初始化书本过滤功能
  function initBookFilter() {
    // 获取所有标签按钮和书籍卡片
    const tabButtons = document.querySelectorAll('.tab-button');
    const bookCards = document.querySelectorAll('.book-card');

    if (tabButtons.length === 0 || bookCards.length === 0) {
      // 如果元素尚未就绪，稍后重试
      setTimeout(initBookFilter, 100);
      return;
    }

    // 从URL中获取初始状态参数
    const urlParams = new URLSearchParams(window.location.search);
    let currentStatus = urlParams.get('status') || 'all';

    // 更新标签高亮状态
    function updateActiveTab() {
      tabButtons.forEach(button => {
        const status = button.getAttribute('data-status');
        if (status === currentStatus) {
          button.classList.add('active');
        } else {
          button.classList.remove('active');
        }
      });
    }

    // [修改] 过滤显示的书籍 - 使用 View Transitions API
    function filterBooks() {
      // 定义 DOM 更新逻辑
      const updateDOM = () => {
        bookCards.forEach(card => {
          const status = card.getAttribute('data-status');
          if (currentStatus === 'all' || status === currentStatus) {
            card.classList.remove('hidden');
            card.classList.add('visible');
          } else {
            card.classList.remove('visible');
            card.classList.add('hidden');
          }
        });
      };

      // 检查浏览器是否支持 View Transitions
      // @ts-ignore
      if (document.startViewTransition) {
        // 使用原生 API 处理平滑过渡和布局重排
        // @ts-ignore
        document.startViewTransition(() => {
          updateDOM();
        });
      } else {
        // 降级方案：不支持的浏览器直接切换（无动画但布局正确）
        updateDOM();
      }
    }

    // 更新URL参数
    function updateURL(status) {
      const url = new URL(window.location);
      if (status === 'all') {
        url.searchParams.delete('status');
      } else {
        url.searchParams.set('status', status);
      }
      window.history.replaceState({}, '', url);
    }

    // 为所有标签按钮添加点击事件
    tabButtons.forEach(button => {
      // 移除可能已存在的事件监听器
      const newButton = button.cloneNode(true);
      button.parentNode.replaceChild(newButton, button);

      // 添加新的事件监听器
      newButton.addEventListener('click', (e) => {
        e.preventDefault(); // 阻止默认行为
        currentStatus = newButton.getAttribute('data-status');
        updateActiveTab();
        filterBooks();
        updateURL(currentStatus);
      });
    });

    // 初始化时设置正确的标签状态和过滤
    updateActiveTab();
    // 初始加载不需要动画，直接执行 DOM 更新即可
    bookCards.forEach(card => {
      const status = card.getAttribute('data-status');
      if (currentStatus === 'all' || status === currentStatus) {
        card.classList.remove('hidden');
        card.classList.add('visible');
      } else {
        card.classList.remove('visible');
        card.classList.add('hidden');
      }
    });
  }

  // 使用 Astro 的页面加载事件，确保在页面导航后也能正确初始化
  document.addEventListener("astro:page-load", initBookFilter);

  // 在初始页面加载时初始化
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initBookFilter);
  } else {
    // 如果页面已经加载完成，直接初始化
    initBookFilter();
  }
</script>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  .books-container {
    width: 100%;
  }

  .books-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--p-surface-300);
    overflow-x: auto;
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .books-tabs::-webkit-scrollbar {
    display: none;
  }

  .tab-button {
    padding: 0.6rem 1.2rem;
    border: 1px solid transparent;
    background-color: transparent;
    border-radius: var(--p-border-radius-md);
    cursor: pointer;
    transition: all 0.2s;
    font-weight: 500;
    color: var(--p-text-color);
    text-decoration: none;
    white-space: nowrap;
    position: relative;
  }

  .tab-button:hover {
    background-color: var(--p-surface-100);
  }

  .tab-button.active {
    background-color: var(--p-emerald-100);
    border-color: var(--p-emerald-500);
    color: var(--p-emerald-700);
  }

  .tab-button.active::after {
    content: '';
    position: absolute;
    bottom: -1rem;
    left: 0;
    width: 100%;
    height: 2px;
    background-color: var(--p-emerald-500);
  }

  .tab-count {
    margin-left: 0.5rem;
    background-color: var(--p-surface-300);
    color: var(--p-text-color);
    border-radius: 9999px;
    padding: 0.1rem 0.5rem;
    font-size: 0.8rem;
  }

  .tab-button.active .tab-count {
    background-color: var(--p-emerald-500);
    color: white;
  }

  h2,
  .post-item-footer {
    font-family: var(--font-family-sans);
    font-weight: 700;
  }

  .post-item-date {
    color: var(--text-secondary);
    text-align: left;
    text-transform: uppercase;
    margin-right: 16px;
  }

  .post-category {
    color: var(--p-emerald-500);
    text-align: left;
    text-transform: uppercase;
    margin-right: 16px;
  }

  hr {
    margin: 60px auto;
  }

  .category-nav {
    display: flex;
    gap: 1rem;
    margin-bottom: 2rem;
  }

  .category-link {
    padding: 0.5rem 1rem;
    border: 1px solid var(--p-surface-300);
    border-radius: var(--p-border-radius-md);
    text-decoration: none;
    color: var(--p-text-color);
    transition: all var(--p-transition-duration);
  }

  .category-link:hover {
    background-color: var(--p-surface-100);
    border-color: var(--p-surface-400);
  }

  .category-link.active {
    background-color: var(--p-emerald-100);
    border-color: var(--p-emerald-500);
    color: var(--p-emerald-700);
  }

  .post-item a {
    text-decoration: none;
    color: inherit;
  }

  /* Books grid specific styles */
  .books-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .book-card {
    background: white;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    /* [修改] 移除 opacity/visibility 过渡，只保留 hover 效果的过渡 */
    /* View Transitions API 会处理布局动画 */
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    height: fit-content;
    will-change: transform;
    /* 优化渲染性能 */
    contain: layout paint;
  }

  /* [修改] 使用 display: none 确保 Grid 自动填补空位 */
  .book-card.hidden {
    display: none;
  }

  .book-card.visible {
    display: block;
  }

  .book-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.15);
    /* [修改] Hover 时暂时禁用 view-transition-name 以防冲突 */
    view-transition-name: none;
  }

  .book-card a {
    text-decoration: none;
    color: inherit;
    display: block;
  }

  .book-cover {
    width: 100%;
    height: 280px;
    object-fit: contain; /* 改为contain以完整显示图片，而不是裁剪 */
    display: block;
    background-color: #f8f9fa; /* 添加浅色背景 */
    border-radius: 4px; /* 保持边角圆润 */
  }

  .book-cover-placeholder {
    width: 100%;
    height: 280px;
    background-color: #e0e0e0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #666;
    font-style: italic;
  }

  .book-info {
    padding: 1rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
  }

  .book-info h3 {
    margin: 0 0 1rem 0;
    font-size: 1.1rem;
    line-height: 1.4;
  }

  .book-status {
    margin-bottom: 0.5rem;
  }

  .status-badge {
    display: inline-block;
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.8rem;
    font-weight: bold;
    text-transform: uppercase;
  }

  .status-to-read {
    background-color: #e3f2fd;
    color: #1976d2;
  }

  .status-reading {
    background-color: #fff8e1;
    color: #ff8f00;
  }

  .status-finished {
    background-color: #e8f5e9;
    color: #388e3c;
  }

  .status-abandoned {
    background-color: #ffebee;
    color: #d32f2f;
  }

  .finish-date {
    margin-top: 0.5rem;
    font-size: 0.8rem;
    color: #666;
    font-style: italic;
  }

  .book-rating {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .stars {
    display: flex;
  }

  .star {
    color: #ddd;
    font-size: 1.2rem;
  }

  .star.filled {
    color: #ffc107;
  }

  .rating-value {
    font-size: 0.9rem;
    color: #666;
    margin-left: 0.5rem;
  }

  @media (max-width: 768px) {
    .books-grid {
      grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
      gap: 1.5rem;
    }

    .book-cover {
      height: 220px;
    }

    .book-info {
      padding: 0.75rem;
    }

    .book-info h3 {
      font-size: 1rem;
    }
  }
</style>